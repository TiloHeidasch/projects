# Secrets
.env
.env.bck.*

# Runtime files (generated on each run)
last_cmd.log
last_result.json
started_at
has_build
webui_url
version

# Docker Compose lock files
.compose.lock

# Build source directories (cloned repos for build-from-source stacks)
odysseus/build-src/
